{{#each article}}
<div class="card">
    <div class="card-header" id="{{_id}}">
     <h3><a class="article-link" target="_blank"  href="{{link}}">{{title}}</a><a class="btn btn-success save">Save Article</a></h3>
     </div>
     <div class="card-body">{{content}}</div>
     </div>
{{/each}}